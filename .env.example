# Copy to .env only for local shell tooling. The project never reads or commits
# a literal key automatically; export these variables before API stages.
OPENLUX_API_KEY=
OPENLUX_BASE_URL=https://api.openlux.ai/v1
OPENLUX_MODEL=gpt-5.6-sol

# External assets are intentionally outside this repository.
MAPTAB_ROOT=../maptab_data
QWEN35_MODEL=/path/to/Qwen3.5-9B
MODEL_PATH=../metromap_final
